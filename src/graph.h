#include <stdio.h>

#include "stack.h"
#include "queue.h"

#ifndef _GRAPH_H_
#define _GRAPH_H_

// a function that constructs an adjacency list of a graph
    // an adjacency list is essentially an array of linked lists
queue* constructAdjacencyList(int numVertices) {
    int i;
    for (i = 0; i < numVertices; i++) {
        // iterate through each line 
    }
}

// a function that constructs an undirected graph given an adjacency list
int constructUndirectedGraph() {

}

#endif